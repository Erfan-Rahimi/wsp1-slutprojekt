<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" type="text/css" href="/style.css?<%=Time.now.to_i%>">
</head>
<body>
    <div class="edit-container">
        <div class="entryarea">
            <form action="/movies/<%= @movie['id'] %>/update" method="post">
                <div class="edit-input">
                    <p>Movie name:</p>
                    <input type="text" id="title" name="movie_name" class="edit_input" value="<%= @movie['title']%>">
                </div>
                <div class="edit-input">
                    <p>Movie genre:</p>
                    <input type="text" id="genre" name="movie_genre" class="edit_input" value="<%= @movie['genre']%>">
                </div>
                <div class="edit-input">
                    <p>Movie price:</p>
                    <input type="text" id="price" name="movie_price" class="edit_input" value="<%= @movie['price']%>">
                </div>
                <div class="edit-input">
                    <p>Movie description:</p>
                    <input type="text" id="movie_description" name="description" class="edit_input" value="<%= @movie['movie_description']%>">
                </div>
                <div class="edit-input">
                    <p>Available: </p>
                    <input type="checkbox" id="available" name="available" class="edit_input" <%= @movie['available_for_rent'] == 1 ? 'checked' : ''%>>
                </div>
                <div id="drop-zone" class="drop-zone">
                    <img src="/img/<%= @movie['image_filename'] %>" id="current-image" class="selected-image">
                </div>
                <input type="hidden" name="image_filename" id="image_filename" value="<%= @movie['image_filename'] %>">
                <div class="image-selection">
                    <% @images&.each do |img| %> <!-- Use safe navigation to prevent errors if @images is nil -->
                        <div class="image-item" draggable="true" data-filename="<%= img %>">
                            <img src="/img/<%= img %>" class="selectable-image" alt="Selectable Movie Poster">
                        </div>
                    <% end %>
                </div>

                <button type="submit">Save</button>
            </form>
        </div>
    </div>
<body>
    <script src="/js/edit.js"></script>
</html>
